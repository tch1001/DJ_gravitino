You are codex-audio, implementing the audio engine of Gravitino DJ. Read CLAUDE.md, docs/ARCHITECTURE.md, docs/STATUS.md first. You own ONLY these files (currently empty): src/audio/AudioEngine.cpp, src/audio/Deck.cpp, src/audio/Eq.cpp, and you may create src/audio/Eq.h and other src/audio/*.h internals. Do NOT edit any other files except updating your row/notes in docs/STATUS.md. Do NOT git commit.

Implement against the PINNED headers src/audio/AudioEngine.h, src/control/ControlBus.h, src/analysis/TrackData.h (do not change them):

- Deck.cpp: pimpl. Fully-decoded track playback from TrackData::pcm (interleaved stereo f32 @48k). Linear-interpolation resampling by tempoRatio (plus transient jog nudge that decays over ~200ms). render() applies trim (0.5=unity, 0=silence, 1=+6dB), 3-band EQ, channel fader. Position kept as atomic<double> frames; positionSec/beatPosition/effectiveBpm per header. Track swap safety: audio thread must never dereference a freed track — e.g. atomic raw pointer swap with GUI-side retention of the old shared_ptr for >2 buffer periods, or stop-swap-restart. No locks/allocations in render().
- Eq.h/.cpp: RBJ biquad low-shelf 250Hz, peaking 1kHz, high-shelf 4kHz. Knob 0..1: 0.5 flat, 0.0 full kill (shelf gain -26dB plus band gain scale to ~0), 1.0 = +6..9dB. Stereo, per-deck, processed inside Deck::render.
- AudioEngine.cpp: pimpl owning ma_device (playback, f32, 2ch, 48000, ~256-frame period; #include "../../third_party/miniaudio.h" with NO implementation define — the implementation is compiled in third_party/miniaudio_impl.c). Callback: render deck A and deck B into scratch buffers (preallocated), equal-power crossfade (xf=0 full A, 1 full B), sum, soft-clip tanh master. renderOffline() runs the exact same mix path headless. applyEvent(): map ControlEvent {Play,Stop,Cue,HotCueN,TempoSync,Tempo(=ratio),Fader,Trim,EqLow/Mid/High,Crossfader,Jog} to deck/engine state; TempoSync sets the deck's tempoRatio so its effective bpm matches the OTHER deck's effective bpm (and phase-nudges to the nearest beat). ControlId::Load: no-op (the transition player pre-loads tracks). Constructor subscribes to bus->eventDispatched (direct connection, GUI thread).

Compile-check ONLY your objects in your OWN build dir:
  cmake -B build-audio -G Ninja
  ninja -C build-audio CMakeFiles/gvtcore.dir/src/audio/AudioEngine.cpp.o CMakeFiles/gvtcore.dir/src/audio/Deck.cpp.o CMakeFiles/gvtcore.dir/src/audio/Eq.cpp.o
(other modules' objects may fail — ignore them). Iterate until your three objects compile warning-clean. Then update docs/STATUS.md: your row to DONE plus brief notes on design choices.
