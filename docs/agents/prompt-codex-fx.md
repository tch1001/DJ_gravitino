You are codex-fx on Gravitino DJ in this repo (read CLAUDE.md, docs/ARCHITECTURE.md, docs/STATUS.md). Implement the per-deck FX insert. You own: src/audio/Deck.cpp, src/audio/AudioEngine.cpp, src/audio/Eq.h/cpp, and may create src/audio/Fx.{h,cpp} (if you add Fx.cpp, append it to the gvtcore sources in CMakeLists.txt, add-only), plus src/midi/MidiEngine.cpp + Flx4Mapping.cpp/h for hardware mapping. Pinned (already committed, do not change): ControlId FxType/FxOn/FxWet/FxBeats with .gvt names fx_type/fx_on/fx_wet/fx_beats; Deck atomics fxType/fxOn/fxWet/fxBeats in src/audio/AudioEngine.h. Do NOT touch src/ui, src/analysis, src/transitions, src/app. Do NOT git commit.

Engine work:
1. FX processing in Deck::render, inserted AFTER the DJ filter and BEFORE the channel fader, fully RT-safe (all buffers preallocated in Deck::Impl at construction; no allocation in render):
   - ECHO (type 0): stereo feedback delay; delay time = fxBeats beats at the CURRENT effective bpm (recompute per render chunk; clamp delay 1ms..2s; preallocate a 2s*48k stereo ring). Feedback 0.5; wet knob mixes echo tail; when fxOn goes false, let the tail ring out (keep processing with input muted into the delay) rather than hard-cutting — standard DJ echo behavior.
   - REVERB (type 1): small Schroeder (4 parallel combs ~29.7/37.1/41.1/43.7 ms with feedback ~0.77, 2 series allpasses 5/1.7 ms g=0.7), stereo by slightly detuning comb lengths per channel; wet mix.
   - FLANGER (type 2): modulated delay 0.5..8 ms, LFO period = fxBeats*4 beats, feedback 0.6, wet mix.
   - fxWet crossfades dry/wet equal-power; fxOn false and tail silent -> bypass (zero extra cost).
2. AudioEngine::applyEvent: FxType (round value to 0..2), FxOn (>0.5), FxWet (clamp 0..1), FxBeats (clamp 0.25..4) -> store into deck atomics. This also silences the current -Wswitch warning about unhandled enumerators.
3. Type/param changes while engaged must not click badly (resetting states on type change is fine).
4. FLX4 mapping: map the BEAT FX section per the official DDJ-FLX4 MIDI list / Mixxx: BEAT FX ON/OFF press = toggle current fxOn (read deck atomic, dispatch inverse). BEAT </> buttons -> FxBeats halve/double (dispatch new value). FX select if the hardware exposes one; LEVEL/DEPTH knob -> FxWet. Route per the FX assign buttons if the FLX4 sends them; otherwise apply to both decks and document the choice in STATUS. LED: FX ON lit when engaged.
Verify: cmake -B build-fx -G Ninja; ninja -C build-fx CMakeFiles/gvtcore.dir/src/audio/Deck.cpp.o CMakeFiles/gvtcore.dir/src/audio/AudioEngine.cpp.o CMakeFiles/gvtcore.dir/src/audio/Eq.cpp.o CMakeFiles/gvtcore.dir/src/midi/MidiEngine.cpp.o CMakeFiles/gvtcore.dir/src/midi/Flx4Mapping.cpp.o (plus Fx.cpp.o if created) — warning-clean with -Wall -Wextra. ALSO write and run a standalone audio proof: small main linking your objects + deps that loads a demo track, engages echo at wet 0.6 for 2s of offline deck render then disengages, and asserts the tail after disengage is non-silent for >=0.5s then decays (print RMS per 0.25s window). Update docs/STATUS.md (mapping + semantics). Report the RMS decay numbers.
